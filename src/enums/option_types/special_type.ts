enum SpecialType {
    REVERSE = "reverse",
    SLOW = "slow",
    FAST = "fast",
    FASTER = "faster",
    LOW_PITCH = "lowpitch",
    HIGH_PITCH = "highpitch",
    NIGHTCORE = "nightcore",
}

export default SpecialType;
